version: '3.8'
services:
  web:
    build: .
    ports:
      - "8501:8501"
    environment:
      - MONGO_URL=${MONGO_URL:-mongodb://mongo:27017/}
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
    depends_on:
      - mongo
  mongo:
    image: mongo:latest
    ports:
      - "27017:27017"
